{% load cc_export_tags %}
{% load i18n %}
{% autoescape off %}

% Add content to table of contents
{% if export_pdf %}
\section{ {% if no_desc %} {% trans "Image Content" %} {% else %} {{content.description|tex_escape}} {% endif %} }
{% endif %}

% Show topic title when exporting
{% if export_pdf %}
{\Large \textbf{ {{content.topic.title}}   }}
{% endif %}

\vskip 1em

% Show description when exporting
{% if export_pdf %}
\textit{~~{% trans "Description" %}:} {{content.description|tex_escape}}
{% endif %}

\vskip 1em

% Show image
\begin{figure}[H]
    \centering
    \includegraphics[max width=\linewidth]{~~{{content.imagecontent.image.url|ret_path}}} % ~~ is escape char
\end{figure}

\vskip 3em

{% endautoescape %}
