{% load cc_export_tags %}
{% autoescape off %}

% show topic title when exporting
{% if export_pdf %}
{\Large \textbf{ {{content.topic.title}}   }}
{% endif %}

\vskip 1em


% Figure
\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{~{{content.imagecontent.image.url|ret_path}}}
    \caption{ {{ content.description|tex_escape }} }
\end{figure}

\vskip 3em

{% endautoescape %}
