{% load cc_export_tags %}
{% load i18n %}
{% autoescape off %}

% Show topic title when exporting
{% if export_pdf %}
{\Large \textbf{ {{content.topic.title}}   }}
{% endif %}

\vskip 1em

% Show description when exporting
{% if export_pdf %}
\textit{~~{% trans "Description" %}:} {{content.description|tex_escape}}
{% endif %}

\vskip 1em

% YouTube link
\url{ {{ content.ytvideocontent.url }} }

Starting at: {% if start_hours > 0 %}{{ start_hours }}H {% endif %}{% if start_minutes > 0 %}{{ start_minutes }}M {% endif %}{{ start_seconds }}S\\
Ending at: {% if end_hours > 0 %}{{ end_hours }}H {% endif %}{% if end_minutes > 0 %}{{ end_minutes }}M {% endif %}{{ end_seconds }}S

\vskip 3em

{% endautoescape %}
