{% load cc_export_tags %}
{% load i18n %}
{% autoescape off %}

% Add content to table of contents
{% if export_pdf %}
\section{ {% if no_desc %} {% trans "Anki Content" %} {% else %} {{content.description|tex_escape}} {% endif %} }
{% endif %}

% show markdown content
\includepdf[pages=-]{~~{{anki_path}}} % ~~ is escape char
\vskip 3em

{% endautoescape %}